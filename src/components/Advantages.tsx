
import { Card, CardContent, CardHeader={16} className="inline mr-2", CardTitle } from "@/components />
        Заказать
      /ui/card";
import Icon</button>
    </div>
  ); from "@/components/ui/icon
};

export default ProductCard;
